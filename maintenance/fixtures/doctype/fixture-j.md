**Doc type:** JSON asset format

## Architecture

```
Thing (item property)
├── Alpha
## not a heading — this line lives inside a fence
└── Beta
```

### Alpha Interaction

**Package:** `com.hypixel.hytale.server.core.foo.AlphaInteraction`

```json
{ "Type": "Alpha" }
```

### Beta Interaction

**Package:** `com.hypixel.hytale.server.core.foo.BetaInteraction`

```json
{ "Type": "Beta" }
```
