# Page-size arrears — pages deliberately left above the split threshold.
#
# Parsed by verify-docs.sh's "[HARD] Page-size arrears list is current" gate.
# CLAUDE.md invariant 5 carries the rule and the reasoning; this file is the
# list itself, so that a checker can read it and so that no size figure has to
# be kept in prose.
#
# FORMAT: one bare page filename per line (relative to docs/). Everything from
# a '#' to end of line is a comment. Blank lines ignored.
#
# NO SIZES HERE. A recorded size went stale twice on 2026-09-04; the gate
# prints the live one beside each entry. This file carries only the two facts a
# human decides: which pages are deferred, and where they cut.
#
# To ADD an entry: the page is over the threshold and you are deferring the
# split. To REMOVE one: the split landed, or the page dropped under. The gate
# warns in both directions, so neither can go quiet.

blocks.md
#   seam: the connected-block sections (~380 lines), and `## Java API Reference`
#   onward (~1,100).

interactions-flow.md
#   seam: the cooldown cluster — `## CooldownCondition`, `## TriggerCooldown`,
#   `## ResetCooldown`, `## IncrementCooldown` — contiguous, in that order (no
#   size here, per this file's own rule). They are the only four types on the
#   page that act on the CooldownHandler rather than on the chain, they already
#   cross-reference each other from their "Related Interactions" lists, and the
#   cut leaves the conditions and the control-flow types where they are.
#   ANCHOR COST — 8 incoming cross-doc links from exactly two files:
#   interactions.md (7 — the four registry rows, the type list at :112, and two
#   on :881, where one line links #cooldowncondition twice) and 01-index.md:112.
#   No other page links into the cluster. The 8 was produced by RUNNING the line
#   below, not by writing it: the first version of this grep returned 3 through a
#   bad alternation, so re-derive rather than trust the count.
#     grep -rho 'interactions-flow\.md#\(cooldowncondition\|triggercooldown\|resetcooldown\|incrementcooldown\)' docs/ | wc -l
#   Recorded 2026-09-04 while adding IncrementCooldown, because growing a page
#   whose seam is unrecorded is how an arrear goes quiet.

ui-api.md
#   seam: not yet recorded.

inventory.md
#   seam: `## Crafting System` onward (~600).

world.md
#   seam: two contiguous cuts — `## WorldChunk` through `## ChunkTracker` (625
#   lines, the chunk API), and `## World Events` + `## Chunk Events` (260 lines,
#   1488-1747). Leaves ~875.
#   `## Chunk Events` goes with `## World Events`, NOT with the chunk API: they
#   are adjacent, both document universe.world.events/.events.ecs, and both are
#   event catalogs where the chunk sections are class reference.
#   ANCHOR COST — the chunk seam is the expensive half. Five other pages point
#   into it at #worldchunk (universe-saves.md:446, collision.md:174,
#   prefabs.md:200, fluids.md:232, blocks.md:120), plus world.md:134 ->
#   #getchunkflags whose source stays behind. The events seam costs one line,
#   world.md:1102 -> #world-events. world.md:411 -> #chunk-accessors SURVIVES
#   and must not be "fixed": both endpoints move together.
#   NAME COLLISION — the events page cannot be called world-events.md; that is
#   an unrelated subsystem (builtin.adventure.worldevents). The overlap is the
#   phrase only, verified.
#   TAG TRAVELS WITH THE REMAINDER — world.md was retagged
#   `Java API + JSON asset format` on 2026-09-04 when `### RevealMapMarkersInView`
#   (a JSON key table) landed under `## World Map Markers`. That section stays in
#   the remainder, so the remainder keeps the combined tag and the two split-off
#   pages take `Java API`. Do not let the split silently drop it. Read the tag
#   narrowly, though: this page is 75 java fences against 1 json fence, and the
#   JSON half rests on that single six-key table. It does not promise broad JSON
#   coverage, which is why the page carries no **Assets:** field.

prefabs.md
#   seam: `## Prefab File Format` through `## Integration Points` (~343).
